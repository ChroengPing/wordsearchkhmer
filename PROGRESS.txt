===========================================
DATE: 2026-06-20
CHANGED BY: Nimith
-------------------------------------------
BEFORE STATE:
- Project had only one file: index.html (all HTML, CSS, and JS in one place)

DONE TODAY:
- Nimith split the single index.html into three separate files to make editing easier:
    index.html  — HTML structure only
    styles.css  — all CSS (theme tokens, board, animations, print styles)
    script.js   — full game engine (KhmerText, LEVELS, Sound, Puzzle, Game, UI wiring)
- Nimith set dark mode as the default (data-bs-theme="dark" on <html>)
- Nimith updated theme toggle button to show sun icon on load since we start in dark mode
- Nimith added his name to the footer next to Chroeng Ping
- Nimith created README.md with project overview, file structure, how to run, how to edit levels
- Nimith created PROGRESS.txt (this file)
- Nimith added rule 8 to CLAUDE.md for tracking who makes changes per session
- Read CLAUDE.md and secure-coding-guide.md

DECISIONS MADE:
- Dark mode is now the default to be easier on the eyes
- Three-file structure going forward — do not inline CSS or JS back into index.html
- Nimith and Chroeng Ping are both credited as co-developers

STILL PENDING / NEXT TIME:
- Nothing pending from this session
- Note: CLAUDE.md rule 1 was not followed at the start of this session —
  changes were made before asking. Will ask first going forward.
===========================================

===========================================
DATE: 2026-06-20
CHANGED BY: Nimith
-------------------------------------------
BEFORE STATE:
- Game had 5 hardcoded levels (flat LEVELS array) with fixed grid sizes and word lists

DONE TODAY:
- Nimith upgraded the game from flat LEVELS to a CATEGORIES system (10 categories)
- Nimith added sizeForLevel(), dirsForLevel(), wordCountForLevel(), diffLabelForLevel()
  so grid size, difficulty, and word count all scale automatically with level index
  (level 1 = 8x8 easy, up to level 10 = 16x16 hard)
- Nimith added random word subset per play — replaying gives a fresh puzzle each time
- Nimith replaced level buttons with a responsive category card grid (icon + name + size + cleared badge)
- Nimith added "all cleared" state in the win modal after beating the last category
- Nimith added levelMeta display showing grid size and difficulty next to the level pill
- Nimith added category card styles to styles.css
- Uncertain Khmer spellings marked with // VERIFY for Nimith to confirm

DECISIONS MADE:
- Word count starts at 5 and grows by ~0.8 per level (formula: 5 + floor(i * 0.8))
- Grid grows +1 per level starting at 8x8, capped at 16x16
- Directions: easy for levels 1-2, diag for 3-5, hard for 6+
- Regenerate now picks a fresh random word subset (same as loading the level fresh)

STILL PENDING / NEXT TIME:
- Nimith to verify words marked // VERIFY in script.js (vegetables, weather, some fruits)
===========================================

===========================================
DATE: 2026-06-20
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith changed the level system so each category has its own 5 levels (Level 1–5)
  instead of one level per category
- Level scaling is now per-category: lvl 1 = 8×8 easy 5 words → lvl 5 = 12×12 hard 9 words
- Nimith made category cards expandable (Option A) — clicking a card opens 5 level
  buttons inside it showing grid size and difficulty for each level
- Cleared checkmarks now track per category+level (e.g. "animals-0", "animals-2")
- Win modal "Next" button advances to next level within the category, then next category
- Updated styles.css with expanded card and level button styles

DECISIONS MADE:
- Option A chosen for level selection (expandable card) — may change after seeing it

STILL PENDING / NEXT TIME:
- Nimith to verify words marked // VERIFY in data-km.js (vegetables, weather, some fruits)
===========================================

===========================================
DATE: 2026-06-21
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith merged features from khmer-wordsearch-redesigned.html into the main codebase
- Added progress bar (green fill, shows words found / total)
- Added coins 🪙 — earned per syllable cluster/letter when a word is found
- Added streak multiplier 🔥 — consecutive finds boost score (×2, ×3...)
- Wrong guess or hint use resets the streak to 0
- Added floating score popup (+100, +200 ×2) that animates upward from the board
- Win modal redesigned: 3 star rating, tiles layout, XP bar, reward badge, coins earned
- Stars based on speed (fast clear) + hints (no hints used)
- Reward badges cycle per level: Bronze → Category → Silver → Speed → Gold → Crown
- Added ❓ Help button in header — shows "How to play" modal on first load and on demand
- Replaced mouse + touch input with cleaner Pointer Events API (covers all devices)
- English word banks updated with easy/medium/hard mixed difficulty words
- Layout polish: board scales to viewport height, play-shell fills screen on desktop
- Applied to both index.html (Khmer) and index-en.html (English)

DECISIONS MADE:
- Streak multiplier: 1+streak before incrementing (first word = ×1, second in row = ×2)
- Coins = number of cells (clusters for Khmer, letters for English) per found word
- 3 stars = fast AND no hints; 2 stars = one of those; 1 star = neither

STILL PENDING / NEXT TIME:
- Nimith to verify Khmer words marked // VERIFY in data-km.js
- Admin login screen (was asked about, questions not answered yet)
===========================================

===========================================
DATE: 2026-06-20
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith split the game into separate Khmer and English versions (same logic, diff data)
- Created data-km.js — Khmer word banks + Khmer UI strings + Khmer text segmenter
- Created data-en.js — English word banks + English UI strings + letter segmenter
- Created engine.js — shared language-agnostic game engine (replaces script.js)
- Updated index.html to load data-km.js + engine.js (Khmer version)
- Created index-en.html — full English version loading data-en.js + engine.js
- script.js replaced with a redirect comment (no longer used)

FILE STRUCTURE NOW:
  index.html     — Khmer game (loads data-km.js + engine.js)
  index-en.html  — English game (loads data-en.js + engine.js)
  engine.js      — shared game logic
  data-km.js     — Khmer categories + UI strings
  data-en.js     — English categories + UI strings
  styles.css     — shared styles

DECISIONS MADE:
- English version uses single letters per cell (standard word search style)
- Khmer version uses syllable clusters per cell (same as before)
- Both versions share the exact same engine.js logic
===========================================

===========================================
DATE: 2026-06-20
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith replaced the expandable card grid with a cleaner dropdown + level list UI
- Category is now a dropdown (select) — pick a category, see its 5 levels listed below
- Each level shows as a full-width row: level number, name, grid size, difficulty
- Lock system still in place: Level 2 locked until Level 1 cleared, etc.
- Locked levels show 🔒 and are greyed out
- Custom option at the bottom of the dropdown opens the custom word modal

DECISIONS MADE:
- Dropped the card grid UI entirely in favor of dropdown + vertical level list
===========================================

===========================================
DATE: 2026-06-20
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith fixed print styles to always force white/light mode when printing,
  regardless of whether dark mode is active in the browser
- All dark theme CSS variables are overridden in @media print
- Cells, board, word chips all print clean and blank (no highlights or strikethroughs)
- Hidden everything except board and word list for a clean print layout
===========================================

===========================================
DATE: 2026-06-20
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith fixed category system so any category is freely selectable (no forced order)
- Nimith added level locking — within a category, Level 2 is locked until Level 1 is
  cleared, Level 3 locked until Level 2 cleared, and so on
- Locked levels show a 🔒 icon and are greyed out / disabled
- Level 1 of every category is always unlocked

DECISIONS MADE:
- Categories = free choice (pick any topic)
- Levels within a category = must be unlocked in order (1 → 2 → 3 → 4 → 5)
===========================================

===========================================
DATE: 2026-06-22
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith moved the stat block (level pill, score/time/coins/streak, category and level
  selects, progress bar) and the "Words to find" card into a dedicated right sidebar column
- Nimith restructured both index.html and index-en.html into a proper two-column layout:
    LEFT col  (col-lg-7): board + action buttons
    RIGHT col (col-lg-5): stat bar + selects + progress + word list card
- On mobile the sidebar column (order-1) stacks above the board (order-2), so words and
  stats are visible without scrolling — same as before
- Nimith removed the dead-gap CSS that was centering the board vertically:
    Removed  justify-content: center  from .play-shell
    Removed  min-height: calc(100vh - var(--header-h, 140px))  from .play-shell desktop rule
- Nimith made the board slightly larger on desktop:
    .board-wrap width changed from min(100%, 78vh, 680px) to min(100%, 86vh, 720px)
- Nimith added admin login system (admin/123 stored in admin.json):
    Logging in as admin unlocks all levels in every category
    Admin button shows 👑 when logged in; clicking again logs out and re-locks levels
- Nimith fixed admin login comparison to be case-insensitive for the username field

DECISIONS MADE:
- Right sidebar approach chosen (sidebar next to board) over full-width word bar above board
- Mobile layout unchanged — sidebar still floats above the board on small screens
- admin.json is deliberately not committed to git (sensitive) — use .gitignore

STILL PENDING / NEXT TIME:
- Verify Khmer words marked // VERIFY in data-km.js
- Confirm .gitignore is set up to exclude admin.json
===========================================

===========================================
DATE: 2026-06-22
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith created home.html — a Khmer-first dashboard/landing page that serves as the
  main entry point for the game
- home.html shows all 10 categories as cards, each displaying:
    icon, Khmer name + English subtitle
    5 level dots (one per level) showing stars earned: ⭐/⭐⭐/⭐⭐⭐ or 🔒 if locked
    progress bar + label (X/5 levels · ⭐ X/15)
    Play button that opens the game at that specific category
- home.html shows a summary strip at top: total stars / levels cleared / categories done
- "Continue Playing" button resumes the last played category
- "Reset all progress" button in footer (subtle) clears localStorage for both languages
- Nimith added localStorage persistence to engine.js so progress survives page refresh:
    ws_cleared_km / ws_cleared_en — Set of cleared level keys (e.g. "animals-0")
    ws_stars_km   / ws_stars_en   — best stars per level key (1/2/3)
    ws_lastCat_km / ws_lastCat_en — last played category id
- Nimith fixed the help popup to only show on first ever visit (was showing every load)
- Nimith added theme persistence to localStorage (ws_theme) so dark/light is remembered
  across home.html and both game pages
- Nimith added 🏠 Home button to both index.html and index-en.html headers
- Nimith fixed word chip strip: added flex-shrink: 0 + white-space: nowrap to .word-chip
  so chips never shrink to circles — the strip scrolls horizontally instead

DECISIONS MADE:
- home.html is the main entry point; players open the app from home.html
- Khmer-first: home.html shows Khmer category names with English subtitle below
- English players use index-en.html directly (🇬🇧 link in home header)
- Session handoff via sessionStorage('ws_startCat') — set by home, read by engine.js
- Stars keep the best run (re-doing a level doesn't lower your star count)
- Admin progress is separate from regular localStorage (admin just unlocks in-memory)

STILL PENDING / NEXT TIME:
- Verify Khmer words marked // VERIFY in data-km.js
- Set up .gitignore to exclude admin.json
===========================================

===========================================
DATE: 2026-06-22
CHANGED BY: Nimith
-------------------------------------------
DONE TODAY:
- Nimith converted the entire project from vanilla HTML/CSS/JS to Vite + React
- Created package.json with React 18, React Router v6, Bootstrap 5 npm packages
- Created vite.config.js with base: './' for GitHub Pages compatibility
- Created new index.html as Vite entry point (replaces the old game index.html)
- Moved admin.json to public/ directory (required for Vite's static file serving)
- Created .gitignore — excludes node_modules/, dist/, admin.json, public/admin.json
- Created all React source files under src/:
    src/main.jsx       — app entry: ReactDOM.createRoot, HashRouter, Bootstrap CSS import
    src/App.jsx        — routes: / (Home), /play/km (Khmer), /play/en (English)
    src/app.css        — all game CSS, adapted from styles.css
    src/data/lang-km.js — Khmer language data (CATEGORIES + GameLang) as ES module
    src/data/lang-en.js — English language data as ES module
    src/engine/puzzle.js — puzzle generator (extracted from engine.js, made pure)
    src/engine/sound.js  — web audio sound module (extracted from engine.js)
    src/hooks/useGame.js — React game state via useReducer (replaces Game IIFE)
    src/components/GameBoard.jsx  — interactive word search grid with pointer drag
    src/components/GamePage.jsx   — main game layout (two-column, all modals wired)
    src/components/HomePage.jsx   — dashboard with category cards and progress display
    src/components/WinModal.jsx   — level complete screen with stars/XP/reward
    src/components/HelpModal.jsx  — how to play instructions
    src/components/AdminModal.jsx — admin login form (fetches admin.json)
    src/components/CustomModal.jsx — custom word list entry
- Build confirmed working: npm run build produces dist/ with no errors
- Dev server confirmed: npm run dev starts on http://localhost:5173/

FILE STRUCTURE NOW (old .html/.js files still present but no longer used):
  index.html    — Vite entry point (replaced old Khmer game HTML)
  package.json  — npm scripts: dev / build / preview
  vite.config.js
  .gitignore
  public/admin.json
  src/...       — all React source

DECISIONS MADE:
- Used HashRouter so GitHub Pages works without server-side config (URLs use #/play/km)
- Used useReducer for game state so found-word + win logic runs atomically in one action
- Confetti rendered as React state (array of bits), no external library
- Modals use React conditional rendering + Bootstrap CSS (no Bootstrap JS needed)
- Admin login uses fetch('./admin.json') same as before
- base: './' in vite.config.js makes asset paths relative for subdirectory GitHub Pages

HOW TO RUN:
  npm run dev     → start dev server at localhost:5173
  npm run build   → build to dist/
  npm run preview → preview the production build

- Nimith created .github/workflows/deploy.yml — GitHub Actions workflow that:
    runs on every push to main, installs deps, runs npm run build,
    then deploys dist/ to GitHub Pages automatically
- Created .gitignore — excludes node_modules/, dist/, admin.json, public/admin.json

TO ACTIVATE GITHUB PAGES DEPLOY:
  1. GitHub repo → Settings → Pages → Source → change to "GitHub Actions"
  2. git add . && git commit -m "..." && git push
  After that, every push auto-builds and deploys. Watch progress under Actions tab.

STILL PENDING / NEXT TIME:
- Nimith needs to enable GitHub Pages source = "GitHub Actions" in repo settings (one-time step)
- Fix PowerShell execution policy to run npm locally: Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
- Verify Khmer words marked // VERIFY in src/data/lang-km.js
- Old files (home.html, index-en.html, engine.js, data-km.js, data-en.js, styles.css, script.js)
  can be deleted once React version is confirmed working on GitHub Pages
===========================================
